{
  "name": "express-server-template",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/app.ts",
    "build": "rm -rf build && tsc -p . && cp package.json ./build && mkdir -p ./build/public",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.20",
    "@types/express-session": "^1.18.1",
    "@types/multer": "^1.4.12",
    "@types/node": "^20.8.7",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "axios": "^1.5.1",
    "dayjs": "^1.11.10",
    "express": "^4.18.2",
    "express-session": "^1.18.1",
    "mariadb": "^3.4.0",
    "multer": "1.4.5-lts.1",
    "pino": "^8.16.0",
    "pino-pretty": "^10.2.3"
  }
}
